---
# Causa MCP Server — OpenShift Route
# Namespace placeholder: PLACEHOLDER_NAMESPACE  (replaced by installer at apply time)
#
# Exposes the Causa MCP Server externally via the OpenShift ingress router.
# TLS is terminated at the router (edge termination).
#
# Access after install:
#   https://<host>/mcp
#   (host is assigned by the cluster ingress controller)
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: causa-mcp
  namespace: PLACEHOLDER_NAMESPACE
  labels:
    app: causa-mcp
    app.kubernetes.io/component: causa-mcp
spec:
  to:
    kind: Service
    name: causa-mcp
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
